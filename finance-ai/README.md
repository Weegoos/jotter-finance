# 🤖 pAIda — Финансовый AI-ассистент

FastAPI-сервис для финансового AI-ассистента **pAIda** в приложении Jotter Finance.

## 🎯 Что это?

**pAIda** — это умный финансовый помощник, который:
- 💬 Отвечает на вопросы о личных финансах
- 🌍 Понимает **казахский**, **русский** и **английский** языки
- ✍️ Понимает опечатки и разговорную речь

---

## 🚀 Быстрый старт

### 1. Настройка окружения

```bash
cd finance-ai

# Создайте .env файл
cp env.example .env

# Заполните API ключ в .env:
# ALEM_API_KEY=your_api_key_here
```

### 2. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv .venv

# Активируйте его
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 3. Запуск сервиса

```bash
# Development режим (с автоперезагрузкой)
uvicorn main:app --reload --host 0.0.0.0 --port 2500

# Или напрямую через Python
python main.py
```

### 4. Проверка работы

- Swagger UI: http://localhost:2500/docs
- ReDoc: http://localhost:2500/redoc
- Health check: http://localhost:2500/health

---

## ⚙️ Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `ALEM_API_KEY` | API ключ для AlemLLM | `sk-xxx...` |
| `ALEM_BASE_URL` | URL Alem API | `https://llm.alem.ai/v1` |
| `PRIMARY_LLM_MODEL` | Модель по умолчанию | `alemllm` |

---

## 📡 API Endpoints

### 💬 Чат

#### `POST /llm/smart-chat` — Основной чат с pAIda

**Запрос:**
```json
{
  "message": "Как составить бюджет на месяц?",
  "conversation_history": [],
  "conversation_id": null,
  "model": "alemllm",
  "temperature": 0.7
}
```

**Ответ:**
```json
{
  "thinking_steps": [
    "Анализирую запрос о составлении бюджета",
    "Подбираю практические рекомендации",
    "Формирую структурированный ответ"
  ],
  "message": "Для составления месячного бюджета рекомендую...",
  "model": "alemllm",
  "conversation_id": null,
  "generated_topic": "Составление месячного бюджета"
}
```

**Параметры:**
| Поле | Тип | Описание |
|------|-----|----------|
| `message` | string | Сообщение пользователя (обязательно) |
| `conversation_history` | array | История диалога для контекста |
| `conversation_id` | string | UUID чата (для продолжения диалога) |
| `model` | string | Модель LLM (по умолчанию `alemllm`) |
| `temperature` | float | Креативность 0-2 (по умолчанию 0.7) |

---

#### `POST /llm/smart-chat/stream` — Стриминговый чат (SSE)

Ответ в реальном времени, токен за токеном.

**События потока:**

```
data: {"type": "thinking", "steps": ["Анализирую...", "Подбираю...", "Формирую..."]}

data: {"type": "topic", "topic": "Составление бюджета", "conversation_id": null}

data: {"type": "content", "delta": "Для "}
data: {"type": "content", "delta": "составления "}
data: {"type": "content", "delta": "бюджета..."}

data: {"type": "done", "status": "completed"}
```

---

### ℹ️ Информация

#### `GET /paida/info` — Информация о pAIda

```json
{
  "name": "pAIda",
  "description": "Финансовый AI-ассистент Jotter Finance",
  "welcome_message": "Привет! 👋 Я pAIda — твой персональный финансовый ассистент...",
  "capabilities": [
    "Анализ личных финансов",
    "Бюджетирование",
    "Советы по инвестициям",
    "Финансовое планирование",
    "Управление расходами"
  ],
  "supported_languages": ["Русский", "Қазақша", "English"]
}
```

#### `GET /health` — Health check

```json
{"status": "ok"}
```

---

### 🧪 Тестирование

#### `POST /test/alemllm` — Тест API ключа AlemLLM

```json
{
  "message": "Привет!",
  "max_tokens": 100,
  "temperature": 0.7
}
```

---

## 🧠 Особенности pAIda

### Автогенерация топиков

При **первом сообщении** (пустая `conversation_history`) автоматически генерируется название чата:

- `"Как составить бюджет"` → **"Составление семейного бюджета"**
- `"менің табысым қанша"` → **"Жеке табысты есептеу"**
- `"how to save money"` → **"Effective money saving strategies"**

### Шаги мышления

Каждый ответ включает 3-5 шагов мышления (как "thinking" в ChatGPT):

```json
"thinking_steps": [
  "Анализирую запрос о депозитах",
  "Вспоминаю ключевые особенности банковских вкладов",
  "Формирую понятный ответ с примерами"
]
```

### Мультиязычность

pAIda автоматически определяет язык и отвечает на нём:

| Язык | Пример запроса | Ответ на |
|------|----------------|----------|
| 🇷🇺 Русский | "как экономить деньги" | Русском |
| 🇰🇿 Казахский | "ақшамды қалай үнемдеймін" | Казахском |
| 🇬🇧 English | "how to budget" | English |

### Понимание опечаток

pAIda понимает сообщения с опечатками:
- `"скока я потратил за месец"` → понимает как "сколько я потратил за месяц"
- `"инвистиции в АФФ"` → понимает как "инвестиции в ETF"

---

## 📁 Структура проекта

```
finance-ai/
├── main.py           # FastAPI приложение и эндпоинты
├── config.py         # Настройки и переменные окружения
├── llm_client.py     # Клиент для работы с AlemLLM
├── prompts.py        # Системные промпты pAIda
├── schemas.py        # Pydantic модели
├── requirements.txt  # Зависимости Python
├── env.example       # Пример .env файла
└── README.md         # Документация
```

---

## 🔗 Интеграция с finance-server

Сервис `finance-ai` работает как отдельный микросервис. Основной бэкенд (`finance-server`) вызывает его API для AI-функций:

```
[finance-client] → [finance-server:3000] → [finance-ai:2500] → [AlemLLM API]
```

---

## 🐛 Решение проблем

### API ключ не работает
```bash
# Проверьте ключ через тестовый эндпоинт
curl -X POST http://localhost:2500/test/alemllm \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет!"}'
```

### CORS ошибки
По умолчанию разрешён только `http://localhost:9000`. Для других origins измените в `main.py`:
```python
allow_origins=["http://localhost:9000", "http://your-domain.com"]
```

### Порт занят
```bash
# Запустите на другом порту
uvicorn main:app --port 2501
```
