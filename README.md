# 📘 Jotter Finance  
### Personal Finance Tracker — Dev version **1.2.0** (released 14.12.2025)

Jotter Finance — это приложение для учёта личных финансов, управления счетами, транзакциями, бюджетами и анализа расходов.  
В версии **1.2.0** добавлена расширенная интеграция с ИИ: персональные советы, анализ транзакций и умные подсказки по бюджетам.

---

## 🚀 Текущий статус проекта

### 🔥 Новое в версии **1.2.0**
- Расширена ИИ-интеграция: улучшены модели советов и контекстные подсказки по транзакциям и бюджетам.
- Добавлены новые статистические эндпоинты для счётов и транзакций (`/stats/...`).
- Добавлена поддержка ИИ-анализа транзакций через `/ai/advice`.
- Улучшены эндпоинты для работы с ИИ-диалогами: `/conversation` и `/message`.

### Было добавлено в версии **1.1.0**
- ИИ-чат-бот **pAIda**.
- ИИ-ассистент для финансовых советов (`/ai/advice`).
- Улучшенная структура API, оптимизации и мелкие улучшения.

### Базовый функционал
- Пользователи и авторизация (JWT).
- Управление счетами и банками.
- Доходы / расходы и история транзакций.
- Категории и бюджеты.
- Диаграммы и статистика на фронтенде.
- Полноценная связка **NestJS + Quasar**.

---

## 🔧 Технологический стек

### Backend (NestJS)
- NestJS + TypeScript
- PostgreSQL / MySQL / SQLite
- Prisma или TypeORM
- JWT (Passport.js)
- Swagger API
- Интеграция LLM (pAIda + AI Advice Engine)

### Frontend (Quasar, Vue 3)
- Quasar Framework
- Vue 3 (Composition API)
- Pinia
- ApexCharts
- Интерфейс AI-чата и экраны статистики

---

## 📡 API Endpoints

### Auth
- `POST /auth/register`
- `POST /auth/login`
- `GET /auth/me`

### Accounts
- `GET /accounts` — получить все счета пользователя
- `GET /accounts/{active}` — получить только активные счета
- `POST /accounts` — создать счёт
- `PUT /accounts/{id}` — обновить счёт по ID
- `DELETE /accounts/{id}` — удалить счёт по ID

### Categories
- `GET /categories` — получить все категории пользователя
- `POST /categories` — создать категорию
- `PUT /categories/{id}` — обновить категорию
- `DELETE /categories/{id}` — удалить категорию

### Transactions
- `GET /transactions` — получить все транзакции пользователя
- `POST /transactions` — создать транзакцию
- `PUT /transactions/{id}` — обновить транзакцию по ID
- `DELETE /transactions/{id}` — удалить транзакцию по ID
- `POST /transactions/export/pdf` — выгрузить транзакции в PDF

### Budgets
- `GET /budget` — получить все бюджеты пользователя
- `GET /budget/stats` — статистика по бюджетам
- `POST /budget` — создать бюджет
- `PUT /budget/{id}` — обновить бюджет
- `DELETE /budget/{id}` — удалить бюджет

### Stats
- `GET /stats/goal_progress` — прогресс финансовых целей
- `GET /stats/payment_types` — статистика по способам оплаты
- `GET /stats/transaction_stats` — статистика по типам транзакций
- `GET /stats/account_stats` — статистика по счетам

### Bank
- `GET /bank` — список доступных банков

### 🧠 AI
- `POST /ai/advice` — ИИ-ассистент для анализа транзакций и финансовых советов
- `POST /conversation` — создать AI-диалог
- `GET /conversation` — получить все диалоги пользователя
- `PUT /conversation/{id}` — обновить диалог
- `DELETE /conversation/{id}` — удалить диалог по UUID
- `POST /message` — добавить сообщение в диалог
- `GET /message/{conversationId}` — получить сообщения по ID диалога

---

## 📊 Основной функционал

- Учёт счетов, банков и транзакций
- Аналитика и диаграммы (доходы, расходы, типы платежей, цели)
- Бюджеты и категории с отдельной статистикой
- Общий баланс и статистика по счетам
- **ИИ-чат pAIda** и контекстные диалоги
- **Финансовые советы AI Assistant** на основе транзакций
- Экспорт транзакций в PDF (CSV/OFX — в планах)
- Уведомления и расширенный импорт (в планах)

---

## 📅 Roadmap

### v1.x
- Расширенные отчёты и дашборды
- Улучшение AI Advice и персонализации подсказок
- Пуш-уведомления
- Импорт/экспорт CSV/OFX

### v2.x
- Мультивалютность
- Облачная синхронизация
- Мобильное приложение (Quasar + Capacitor)

---

## 🔨 Установка и запуск

### Backend
cd backend
npm install
npm run start:dev